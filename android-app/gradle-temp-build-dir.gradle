allprojects {
    def buildRoot = System.getenv("RESTROSUITE_ANDROID_BUILD_ROOT") ?: "${rootDir}/.tmp-gradle-build"
    layout.buildDirectory = file("${buildRoot}/${project.path.replace(':', '_')}")
}
