# RestoSuite Import Templates

Use the CSV files as separate sheets in one Excel workbook. Keep these exact sheet names:

- `Menu`
- `Inventory`
- `Recipes`
- `Employees`
- `Customers`

Do not rename the header columns. Ingredient keys used in `Recipes` must match `Inventory.IngredientKey`.
